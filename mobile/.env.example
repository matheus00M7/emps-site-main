# Celular físico: use o IPv4 LAN do computador ou uma URL HTTPS de túnel da API.
# Nunca use localhost aqui. O mapa OpenStreetMap não exige chave de API.
EXPO_PUBLIC_EMPS_API_URL=http://192.168.0.10:3001

# O padrão usa a API real. Mude para true somente para apresentação sem backend.
EXPO_PUBLIC_EMPS_DEMO_MODE=false

# Necessária somente para confirmar pagamentos Stripe reais pelo PaymentSheet.
# Use apenas a chave publicável (pk_test_... ou pk_live_...), nunca a chave secreta.
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=
